{
  "name": "asym-pol-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2025-01-01",
    "deploy": "wrangler pages deploy public --project-name=asym-pol-tracker --branch=main --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
